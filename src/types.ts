export interface SankeyOptions {
  monochrome: boolean;
  color: string;
  textColor: string;
  nodeColor: string;
  nodeWidth: number;
  nodePadding: number;
  iteration: number;
}

export interface SankeyFieldConfig {}
