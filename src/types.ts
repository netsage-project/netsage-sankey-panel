export interface SankeyOptions {
  monochrome: boolean;
  color: string;
  textColor: string;
  nodeColor: string;
  nodeWidth: number;
  nodePadding: number;
  iteration: number;
  valueField: string;
}

export interface SankeyFieldConfig {}
